Reference: https://www.npmjs.com/package/test-publish-es6-package?activeTab=code
https://babeljs.io/docs/babel-cli

  // "scripts": {
  //   "build": "rm -rf lib && babel src --out-dir lib && cp -r src/clients/*/schemas lib/clients/*/ && npx tsc",
  //   "test": "echo \"Error: no test specified\" && exit 1",
  //   "prepublishOnly": "npm run build"
  // },

  